import { ResourceType } from "../types/resourceTypes";

export const resourceEmojis: Record<ResourceType, string> = {
  metal: "🔩", // tornillo
  stone: "⛏️", // roca
  energy: "⚡", // rayo
  crystal: "💎", // diamante
};
